import React from 'react'

export default function About() {
  return (
    // It is like a ghost element, but it is needed in the JSX
    <React.Fragment>
      <h1>About</h1>
      <p>This is the TodoList app made while doing a crash course on ReactJS</p>
    </React.Fragment>
  )
}
